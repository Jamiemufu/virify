model Agent {
  id         String   @id @default(uuid())
  name       String
  contact    String
  whatsapp   String?
  liveChat   String?
  properties Property[]
  listings  Listing[]
  user User[]
}